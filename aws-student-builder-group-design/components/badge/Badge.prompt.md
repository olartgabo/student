Flat, uppercase monospace tag for a short status or category word.

```jsx
<Badge color="orange">Open</Badge>
```

Colors map to the accent set: `orange` (default/brand), `sky`, `green`, `purple`, `neutral` (grey, for muted metadata). Keep contents to one or two words.
