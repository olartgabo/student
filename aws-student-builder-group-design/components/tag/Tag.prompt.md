Pill-shaped chip, body font (not monospace) — distinct from Badge, used for filters and multi-select values rather than status labels.

```jsx
<Tag onRemove={() => {}}>Cloud Computing</Tag>
```

Omit `onRemove` for a plain, non-removable chip.
