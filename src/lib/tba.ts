/**
 * Placeholder copy lives here and nowhere else, so "Por anunciar" can never end
 * up stored in content as if it were a real title.
 */
export const TBA = "Por anunciar";
export const NO_SESSION = "Sin sesión";
